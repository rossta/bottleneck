# Ember.Application.initializer
#   name: 'currentUser'

#   initialize: (container) ->
#     controller = container.lookup('controller:currentUser').set('content', null)
#     container.typeInjection('controller', 'currentUser', 'controller:currentUser')
