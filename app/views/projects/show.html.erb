<% title @project.name %>
<%= render_title_html %>
<%= render 'projects/sub_nav' %>
<section class="row">
  <%= render 'cumulative_flows/show', flow: @flow %>

  <article>
    <h5>Today: <%= today.to_s(:short) %></h5>
    <p>Time zone      : <%= Time.zone %>
    <p>Total WIP      : <%= @project.wip_count(today) %></p>
    <p>Total Capacity : <%= @project.capacity_count(today) %></p>
    <p>Cycle Time     : <%= @project.cycle_time(today) %></p>
    <p>Arrival Rate   :
        <%= @project.wip_count(today - @project.cycle_time(today).days) %>
        /
        <%= @project.cycle_time(today) %>
        =
        <%= @project.arrival_rate(today) %> cards / day</p>
  </article>
</section>
